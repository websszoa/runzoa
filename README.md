# RunZoa (런조아)

전국 마라톤 대회 정보를 한 곳에서 확인하고, 접수 상태와 일정, 즐겨찾기(찜), 알림 기능까지 제공하는 러너 플랫폼입니다.

---

## 1. 서비스 개요 (Overview)

RunZoa(런조아)는 마라톤 참가자들이 대회 정보를 빠르게 찾고  
접수 타이밍을 놓치지 않도록 돕는 마라톤 일정 서비스입니다.

단순한 대회 목록 제공을 넘어,  
**접수 상태 / 즐겨찾기 / 알림 / 후기**까지 연결하는 것을 목표로 합니다.

---

## 2. 서비스 목적 (Goal)

RunZoa는 러너들이 대회 참가 과정에서 겪는 문제를 해결합니다.

- 대회 정보가 여러 사이트에 흩어져 있어 찾기 어렵다
- 접수 시작/마감 날짜를 놓쳐서 참가를 못하는 경우가 많다
- 대회별 후기/코스 정보가 블로그/카페에 분산되어 있다
- 내가 참가하고 싶은 대회를 한 곳에서 관리하기 어렵다

---

## 3. 핵심 가치 (Value Proposition)

RunZoa가 제공하는 핵심 가치는 아래 3가지입니다.

1. **대회 탐색 시간 절약**
2. **접수 실패 방지 (알림 중심)**
3. **후기/댓글 기반 정보 축적**

---

## 4. 주요 기능 (Features)

### 4.1 대회 목록

- 연도 / 월 필터
- 지역 필터
- 거리 필터 (FULL / HALF / 10KM / 5KM)
- 접수 상태 필터 (접수대기 / 접수중 / 접수마감 / 추가접수)
- 정렬 (날짜순 / 인기순 / 마감임박)

### 4.2 대회 상세

- 대회 기본 정보 (날짜, 장소, 종목)
- 접수 링크 제공
- 접수 상태 표시
- 지도 연결 (네이버/카카오)
- 조회수, 좋아요(찜) 수 표시

### 4.3 즐겨찾기

- 로그인 사용자 기준으로 저장
- 찜한 대회 목록 관리
- 찜한 대회 기반 알림(확장 기능)

### 4.4 커뮤니티

- 대회 후기 작성
- 댓글
- 질문 게시판

### 4.5 알림

- 접수 시작 알림
- 마감 임박 알림
- 추가 접수 알림

---

## 5. 사용자 흐름 (User Flow)

1. 사용자는 대회 목록에서 원하는 조건으로 대회를 찾는다
2. 대회 상세 페이지에서 접수 링크와 정보를 확인한다
3. 관심 있는 대회는 찜하기로 저장한다
4. (확장) 찜한 대회 기준으로 접수 알림을 받는다
5. (확장) 대회 참가 후 후기/댓글을 남긴다

---

## 6. 기술 스택 (Tech Stack)

- **Frontend**: Next.js, TypeScript
- **UI**: TailwindCSS, shadcn/ui, lucide-react
- **Backend**: Supabase (Postgres, Auth, RLS)
- **Deploy**: Vercel
- **Mobile**: Expo (WebView)

---

## 7. 프로젝트 구조 (Project Structure)

```
app/

├── (home)/                 # 홈 라우트 그룹
│   ├── layout.tsx
│   └── page.tsx            # 홈페이지
├── (root)/                 # 메인 라우트 그룹
│   ├── layout.tsx
│   ├── contact/page.tsx    # 문의하기
│   ├── favorites/page.tsx  # 즐겨찾기
│   ├── notice/page.tsx     # 공지사항
│   ├── privacy/page.tsx    # 개인정보처리방침
│   ├── profile/page.tsx    # 프로필
│   ├── records/page.tsx    # 기록
│   └── terms/page.tsx      # 이용약관
├── admin/                  # 관리자 라우트 그룹
│   ├── layout.tsx
│   ├── page.tsx            # 관리자 대시보드
│   ├── contact/page.tsx    # 문의 관리
│   ├── marathon/page.tsx   # 마라톤 관리
│   └── member/page.tsx     # 회원 관리
├── auth/
│   └── callback/route.ts   # Supabase 인증 콜백
├── layout.tsx              # 루트 레이아웃
├── globals.css             # 전역 스타일
├── loading.tsx             # 전역 로딩 UI
├── error.tsx               # 전역 에러 바운더리
├── not-found.tsx           # 404 페이지
```

---

## 8. Next.js 프로젝트 생성

```bash
npx create-next-app@latest ./                          # Next.js 프로젝트 생성
npm install @supabase/supabase-js @supabase/ssr        # Supabase 클라이언트 & Supabase SSR 지원
npm install next-themes                                # 다크모드 테마 지원
npx shadcn@latest init                                 # shadcn/ui 초기화
npx shadcn@latest add button
npx shadcn@latest add sonner
npx shadcn@latest add sheet
npx shadcn@latest add scroll-area
npx shadcn@latest add separator
npx shadcn@latest add form
npx shadcn@latest add textarea
npx shadcn@latest add input
npx shadcn@latest add input-otp
npx shadcn@latest add checkbox
npx shadcn@latest add select
npx shadcn@latest add badge
npx shadcn@latest add table
npx shadcn@latest add dialog
npx shadcn@latest add avatar
npx shadcn@latest add card
npx shadcn@latest add dropdown-menu
npx shadcn@latest add tooltip
npx shadcn@latest add sidebar
npx shadcn@latest add carousel
```

## 🔐 소셜 로그인 (Google / Kakao)

구글·카카오 OAuth를 통한 로그인(및 최초 이용 시 자동 회원가입) 프로세스입니다.

```
─────────────────────────────────────────────────────────────────
  1. 로그인 버튼 클릭
     dialog-login.tsx → GoogleLoginButton / KakaoLoginButton
     사용자가 구글 또는 카카오 버튼 선택
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  2. Supabase OAuth 요청
     google-login-button.tsx / kakao-login-button.tsx
     → supabase.auth.signInWithOAuth({ provider: "google" | "kakao" })
     options.redirectTo: ${origin}/auth/callback
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  3. 제공자 로그인 페이지로 리다이렉트
     Google / Kakao 로그인 화면으로 이동
     최초 이용 시: 동의 후 계정 생성 (자동 회원가입)
     기존 사용자: 로그인만 수행
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  4. 콜백 URL로 복귀
     로그인 성공 후 Supabase가 지정한 Redirect URL로 이동
     → /auth/callback?code=xxx&next=/ (next는 로그인 후 갈 경로)
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  5. 콜백 라우트에서 세션 생성 (auth/callback/route.ts)
     → supabase.auth.exchangeCodeForSession(code)
     code로 세션 교환 후 쿠키에 저장
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  6. 탈퇴 계정 여부 확인
     → supabase.rpc("is_my_account_deleted")
     탈퇴한 계정이면 signOut() 후 /?error=deleted_account 로 리다이렉트
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  7. 방문 횟수 증가 (선택)
     → supabase.rpc("increment_visit_count") (실패 시 로그만, 흐름은 계속)
─────────────────────────────────────────────────────────────────
                              ▼
─────────────────────────────────────────────────────────────────
  8. 로그인 완료 리다이렉트
     next 파라미터 경로(없으면 /)로 이동
     ?welcome=true 로 로그인 성공 표시 → auth-toast에서 "로그인이 되었습니다" 토스트
─────────────────────────────────────────────────────────────────
```
